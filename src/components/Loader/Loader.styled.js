import styled from 'styled-components';
import { Dna } from 'react-loader-spinner';

export const Loader = styled(Dna)``;

export const LoaderWrapper = styled.div`
  display: flex;
  justify-content: center;
`;
